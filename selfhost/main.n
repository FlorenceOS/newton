const std = @import("std");

fn main() noreturn {
    const fd = std.os.syscalls.open("selfhost/main.n".&, 0, undefined);
    std.print.log_hex_upper("Open fd".&, fd);
    std.print.flush();
    std.os.syscalls.exit(0);
}
